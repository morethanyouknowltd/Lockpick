/**
 * Babel-transform converts these so that you can always find them, and easily
 * remove all temporary logs if you need to
 */
export default function tempLog(...args: any[]) {
  // console.log(...args)
}
